name: myfirst-action
on: workflow_dispatch
jobs:
  myfirstjob:
    runs-on: ubuntu-latest
    environment: test-env
    steps:
      - name: echo message
        run: echo "Hello, ${{ vars.MY_NAME }}"
      - name: Print surname
        run: echo "${{ secrets.MY_SURNAME }}"
